services:
  app:
    build: .
    restart: unless-stopped
    environment:
      UPLOADS_DIR: /app/uploads
    ports:
      - "3000:3000"
    volumes:
      - uploads_data:/app/uploads

volumes:
  uploads_data:
